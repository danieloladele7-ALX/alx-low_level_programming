#include <stdio.h>
/**
 * main - Entry point
 *
 * code prints out - with proper grammar, but the outcome is a piece of art,
 *
 * Return: Always 0 on Success.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
