#include "main.h"

/**
 * main - tests function for is positive or negative
 * to check if i=0 works correctly.
 *
 * Return: 0
 */
int main(void)
{
	int i = 0;

	positive_or_negative(i);

	return (0);
}
